<%- include("partials/header"); -%>
    <div class="container">
        <% pokemonarr.forEach(pokemon=>{ %>
            <div>
                <img src="<%= pokemon.sprites.normal %>" alt="">
                <div style="margin: auto; text-align: center;"><%= pokemon.name %></div>
            </div>
        <%})%>
    </div>
<%- include("partials/footer"); -%>